require 'spec_helper'

describe RepliconSchedulerClient do
  it 'has a version number' do
    expect(RepliconSchedulerClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
